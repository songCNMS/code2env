# task_coordinator_code2env_coordinator_8b1dc080 - coordinator 永续管理任务

<!-- METADATA:STATUS=InProgress,ASSIGNEE=intern_code2env_coordinator -->

## 背景

这是 coordinator 创建时自动生成的用户级长期管理任务。该任务不代表某个普通开发需求，而是 coordinator 的常驻职责锚点。

## 任务目标

持续承接用户需求，创建和管理 team_lead，跨 workspace 分配任务并跟进结果。

## 生命周期规则

- 只要对应 coordinator 存在，本任务必须保持 InProgress。
- 不允许把本任务标记为 Completed；删除或迁移 coordinator 时应由专门流程处理。
- 本任务属于 coordinator 的 anchor workspace，不表示 coordinator 只管理该 workspace。

## Coordinator

- intern_name: `intern_code2env_coordinator`
- coordinator_id: `code2env`
