# task005_survey_problem_goals_schema

<!-- METADATA:STATUS=Completed,ASSIGNEE=intern_code2env_dev -->

## Background

The project needs a lightweight planning schema for the code-to-env / code2env direction. The immediate request is to create a small `survey/` directory with one initial document that clarifies the problem, inputs, outputs, downstream users/tasks, and goals.

## Goals

- Read the current repository and existing project discussion artifacts.
- Create `survey/01-problem-and-goals.md`.
- Keep the document lightweight and avoid turning it into a full PRD or heavy research survey.
- Ground the initial content in the current code2env implementation and discussions.

## Acceptance

- `survey/01-problem-and-goals.md` exists.
- The document contains only the requested lightweight schema sections.
- The content frames the direction as constructing trainable, evaluable, reproducible agent environments from real repos/packages/services.
- No implementation code is added.
